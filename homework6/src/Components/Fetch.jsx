const Fetch = (props) => {
  //   try {
  //     const data = fetch(url, {
  //       method: "POST", // method is POST as we want to post the data;
  //       body: JSON.stringify(myEvent), // object is stringified;
  //       headers: {
  //         // headers is the info about the doctype;
  //         Authorization: key,
  //         "Content-Type": "application/json",
  //       },
  //     }).then();
  //   } catch (error) {
  //     console.log("There is error in the catch section");
  //   }
  //   return (
  //     <>
  //       <h6>I need to fetch the id:{props.id}</h6>
  //     </>
  //   );
};

export default Fetch;
